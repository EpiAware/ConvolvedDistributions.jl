import{_ as o,o as t,c as a,an as i}from"./chunks/framework.B5ttPJ28.js";const u=JSON.parse('{"title":"Release notes","description":"","frontmatter":{},"headers":[],"relativePath":"release-notes.md","filePath":"release-notes.md","lastUpdated":null}'),s={name:"release-notes.md"};function d(r,e,n,c,l,p){return t(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="Release-notes" tabindex="-1">Release notes <a class="header-anchor" href="#Release-notes" aria-label="Permalink to &quot;Release notes {#Release-notes}&quot;">​</a></h1><p>ConvolvedDistributions.jl follows a hybrid changelog approach:</p><ul><li><p><strong>GitHub Releases</strong>: All releases (automatically generated by TagBot)</p></li><li><p><strong>NEWS.md</strong>: Major releases requiring detailed context (shown below)</p></li></ul><p>See <a href="https://github.com/EpiAware/ConvolvedDistributions.jl/releases" target="_blank" rel="noreferrer">GitHub Releases</a> for minor/patch release information.</p><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><h3 id="Breaking-changes" tabindex="-1">Breaking changes <a class="header-anchor" href="#Breaking-changes" aria-label="Permalink to &quot;Breaking changes {#Breaking-changes}&quot;">​</a></h3><ul><li><strong><code>discretise_pmf</code> is removed.</strong> Discretising a continuous delay is a censoring choice this package does not make; CensoredDistributions.jl owns primary and interval censoring, including double-interval-censored masses. <code>convolve_series</code> still accepts a <code>DiscreteUnivariateDistribution</code> directly, and still accepts a caller-supplied PMF — as a plain vector or wrapped in <code>DelayPMF(masses, interval)</code> for reuse — for a continuous delay&#39;s masses, whoever builds them. Migration: replace <code>discretise_pmf(delay, maxlag; interval)</code> with masses built by CensoredDistributions.jl (or your own CDF-difference computation), then <code>convolve_series(masses, series)</code> or <code>ConvolvedDistributions.DelayPMF(masses, interval)</code>. Closes <a href="https://github.com/EpiAware/ConvolvedDistributions.jl/issues/68" target="_blank" rel="noreferrer">#68</a>.</li></ul><h2 id="0.2.0" tabindex="-1">0.2.0 <a class="header-anchor" href="#0.2.0" aria-label="Permalink to &quot;0.2.0 {#0.2.0}&quot;">​</a></h2><p>Breaking changes relative to 0.1.0, with migration notes:</p><ul><li><p><strong><code>convolve_series</code> no longer discretises continuous delays.</strong> A <code>DiscreteUnivariateDistribution</code> convolves via its own PMF (<code>pdf(delay, k)</code> at integer lags); a continuous delay throws. Migration: discretise explicitly — <code>convolve_series(discretise_pmf(delay, length(series) - 1), series)</code> for interval-censored-secondary masses (exact primary event), or build double-interval-censored masses with CensoredDistributions.jl&#39;s <code>convolve_series</code> extension when the primary event is only known to the day — then convolve the resulting PMF.</p></li><li><p><strong>Family names.</strong> <code>AbstractCombinedDistribution</code> is now <code>AbstractConvolvedDistribution</code>, and <code>TestUtils.test_combined_interface</code> is <code>test_convolved_interface</code>. Migration: rename references; behaviour is unchanged.</p></li></ul><p>Additions and improvements:</p><ul><li><p><code>product(x, y)</code> (public type <code>Product</code>) adds the Mellin convolution <code>Z = X * Y</code> for independent components with non-negative support: <code>LogNormal * LogNormal</code> closed form, AD-safe Mellin quadrature otherwise, exact independent-product moments, and <code>quantile</code> via the Optimization extension. Sign-crossing supports throw and are future work.</p></li><li><p><code>discretise_pmf(delay, maxlag; interval)</code> builds a reusable public <code>DelayPMF</code> (raw CDF-difference masses, clamped at zero, never renormalised) with <code>pdf(pmf, lag)</code> mass reads, and <code>convolve_series</code> accepts a <code>DelayPMF</code> or any raw PMF vector, with masses used exactly as given. A <code>DelayPMF</code> carries its grid width: the series is read at steps of <code>pmf.interval</code>, so weekly-binned masses convolve weekly series.</p></li><li><p>Batched numeric <code>cdf</code>/<code>pdf</code>/<code>logpdf</code> now integrate every point over its own scalar-path window on a shared composite panel grid: batched and scalar log densities agree to well within <code>1e-8</code> (typically near machine precision; previously up to ~<code>2e-3</code> in wide-batch tails) while the batched path remains 1.6-2.2x faster than broadcasting (#29).</p></li><li><p>The AD-safe component hook family and the tape-strip pair moved to <a href="https://github.com/EpiAware/EpiAwareADTools.jl" target="_blank" rel="noreferrer">EpiAwareADTools.jl</a> under underscore-free names, together with the analytic gamma-CDF derivative machinery and its per-backend rules. Wrapper packages now depend on EpiAwareADTools directly and extend its names; this package no longer declares its own hooks. Migration:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Old (<code>ConvolvedDistributions</code>)</th><th style="text-align:right;">New (<code>EpiAwareADTools</code>)</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_cdf_ad_safe</code></td><td style="text-align:right;"><code>cdf_ad_safe</code></td></tr><tr><td style="text-align:right;"><code>_logcdf_ad_safe</code></td><td style="text-align:right;"><code>logcdf_ad_safe</code></td></tr><tr><td style="text-align:right;"><code>_ccdf_ad_safe</code></td><td style="text-align:right;"><code>ccdf_ad_safe</code></td></tr><tr><td style="text-align:right;"><code>_logccdf_ad_safe</code></td><td style="text-align:right;"><code>logccdf_ad_safe</code></td></tr><tr><td style="text-align:right;"><code>_pdf_ad_safe</code></td><td style="text-align:right;"><code>pdf_ad_safe</code></td></tr><tr><td style="text-align:right;"><code>_primal</code></td><td style="text-align:right;"><code>primal</code></td></tr><tr><td style="text-align:right;"><code>_primal_distribution</code></td><td style="text-align:right;"><code>primal_distribution</code></td></tr></tbody></table></li><li><p>The extension <code>quantile</code> is now accurate in the far tails: the solve minimises the log-odds residual rather than the near-flat squared probability residual (relative error at <code>p = 0.999</code> down from ~0.16 to ~4e-6 on the analytic product pair) (#48).</p></li><li><p><code>cdf</code>/<code>pdf</code> no longer throw on distributions whose components are themselves composites (for example a <code>difference</code> of two <code>Convolved</code> totals): composite integration windows recurse over the nested components with union-bound tail trims (#45).</p></li><li><p>The batched <code>cdf</code>/<code>pdf</code>/<code>logpdf</code> methods now differentiate: AD tracers on component parameters survive the final convert (#43), ReverseDiff works with respect to the evaluation points (the per-point assembly no longer mutates tracked storage) (#44), and batched-path AD scenarios run on all six backend tags in CI.</p></li><li><p>Numeric quadrature windows are split at the integration component&#39;s quantiles, so node density follows its mass: heavy-tailed components no longer starve the transition region (worst measured case, a <code>Gamma</code> x <code>LogNormal(0, 1.5)</code> product CDF, improved from ~1.4e-2 absolute error to ~5e-11) and most scalar paths got slightly faster (#49).</p></li></ul><h2 id="0.1.0" tabindex="-1">0.1.0 <a class="header-anchor" href="#0.1.0" aria-label="Permalink to &quot;0.1.0 {#0.1.0}&quot;">​</a></h2><p>Initial release. Raw-distribution convolution machinery for any <code>Distributions.jl</code> univariate distribution, split out of CensoredDistributions.jl:</p><ul><li><p><code>convolved(dists...)</code> (public type <code>Convolved</code>): sums of independent components with analytic fast paths (<code>Normal</code>+<code>Normal</code>, equal-scale <code>Gamma</code>, equal-rate <code>Exponential</code>) and an AD-safe fixed-node Gauss-Legendre quadrature fallback; scalar and batched <code>cdf</code>/<code>pdf</code>/<code>logpdf</code>; exact additive moments.</p></li><li><p><code>difference(x, y)</code> (exported type <code>Difference</code>): the <code>Z = X - Y</code> dual with the <code>Normal</code>-<code>Normal</code> closed form and numeric cross-correlation.</p></li><li><p><code>convolve_series</code>: causal convolution of a numeric series with a delay PMF (the renewal-style observation layer).</p></li><li><p><code>quantile</code> for <code>Convolved</code>/<code>Difference</code> via the Optimization + OptimizationOptimJL extension.</p></li><li><p>The <code>AbstractCombinedDistribution</code> family supertype with shipped <code>TestUtils</code> interface verifiers.</p></li><li><p>AD extensions (ChainRulesCore, ForwardDiff, ReverseDiff, Mooncake, Enzyme) with analytic gamma-CDF rules; gradients tested on all backends in CI.</p></li></ul><p>This file tracks notes for major releases and significant milestones; GitHub Releases (auto-generated from merged PRs) cover every release in between.</p>',16)])])}const m=o(s,[["render",d]]);export{u as __pageData,m as default};
